# Optional. Direct image URLs and the bundled Google Fonts catalog work without keys.
VITE_UNSPLASH_ACCESS_KEY=
# Optional metadata API fallback for developers. Font files use the public CSS endpoint.
VITE_GOOGLE_FONTS_API_KEY=
